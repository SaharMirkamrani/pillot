import { FILTER_CATEGORY, FILTER_SUBCATEGORY, FILTER_PHOTO, FILTER_URGENT, FILTER_CITY, FILTER_STATE } from '../actions';

const filter_reducer = (state, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default filter_reducer;
